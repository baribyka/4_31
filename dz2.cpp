
/*using namespace std;;

int main() {
    setlocale(LC_ALL, "Russian");
    int n, k, s;
        setlocale(LC_ALL, "Russian");
    std::cout << "Введите стоимость одного ноутбука, их количество и процент скидки\n";
    std::cin >> n >> k >> s;
    int z,w,r;
    z = n * k;
    r = 100;
    w = z / r * s;
    int t;
    t = z - w;
    std::cout << t << " цена";
    
   /* long long r;
    setlocale(LC_ALL, "Russian");
    long long gb, b;
    std::cout << "Введите размер одного файла в гб и скорость интернет сосединения в битах в секунду\n";
    std::cin >> gb >> b;
    long long q, f, g, qq, sec;
    double min;
    qq = gb * 8589934592;
    sec = gb / b;
    min = sec / 60;




    std::cout << min << "минут";
    //using namespace std;*/
    
  /*  int s, m, h, ho, hou, mi, se, sec, min, day, days, daym, dayh, dayse, daymi, dayho, daymin, daysec, Q;
    std:cout << "Введите время в секундах прошедшее с начала дня ";
   std::cin >> s;
   Q = 60;
   h = s / Q / Q;
   ho = 0 + h;
   m = s / Q;
   sec = s - m * Q;
   mi = s/Q - h*Q;
   days = 86400 - s;
   daym = days / Q;
   dayse =days- daym * Q;
   dayh = days / Q/Q;
   daymi = daym-dayh * Q ;
   dayho = 0 + dayh;
  
  

   std::cout << ho << "Часов\t" << mi << "Минут\t" << sec <<  "Секунд\n";
   std::cout << dayho << "Часов\t" << daymi << "Минут\t" << dayse << "Секунд до конца дня";*/

   /* std::cout << "У Гарри и Ларри было не больше 10 банок\nСколько банок прострелил Гарри\n";
    int g, l,gb,lb;
    std::cin >> g;
    if (g > 9)
    std::cout << "Ошибка!\n";
    else if (g < 9)
        std::cout << "Сколько банок прострелил Ларри\n";
    std::cin >> l;
    if (l > 9)
    std::cout << "Ошибка!\n";
    else if (l < 9)
    gb = 10 - g;
    lb = 10 - l;
    std::cout << gb << "\tНедострелял банок Гарри\n";
    std::cout << lb << "\tНедострелял банок Ларри\n";
    */
    
  /*  return 0;
}*/

